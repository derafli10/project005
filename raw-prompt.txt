[EXECUTE TASK - SYSTEM REINFORCEMENT DIRECTIVE]

TASK TO EXECUTE:
Task Number: [17.3]

TASK CONTENT (pasted directly from tasks.md — agent should NOT re-open tasks.md, this is the full and current content for this task):
  - 17.3 Create weekly cron job with background queue/worker [TIMEOUT PREVENTION]
    - Create `src/app/api/webhooks/cron/route.ts`
    - Configure Vercel Cron to run every Sunday at 21:00 local time
    - **CRITICAL**: To avoid Serverless Execution Timeouts, do NOT evaluate and loop through all database users sequentially in a single synchronous thread. Instead, offload the processing payload into a background queue/worker engine (e.g., **Inngest** or **Upstash QStash**) to trigger distributed, parallel chunk processing.
    - Save AcademicWrapped record with imageUrl
    - Send in-app notification with preview thumbnail and "Share to Story" CTA
    - _Requirements: 11.1, 11.8_

Context/Notes: [requirements.md 11.1, 11.2, 11.3, 11.4, 11.5, 11.6, 11.7, 11.8, 11.9, 11.10]

CRITICAL AGENT INSTRUCTIONS (DO NOT ASSUME, DO NOT HALLUCINATE):

1. USE PASTED TASK CONTENT: The task content above is already complete and current. Do NOT open or search `tasks.md` — treat the pasted content as the authoritative source for this task's scope, sub-bullets, and dependencies.

2. SCOPED SPEC LOOKUP: In `requirements.md` and `design.md`, locate ONLY the section(s) whose headings/keywords match this task's domain (use the task's own sub-bullet terms as your search keywords first). Do not read either document in full unless the targeted section is insufficient to proceed — if so, expand scope one section at a time and state why.

3. TARGETED SCHEMA & CODE INSPECTION: In `prisma/schema.prisma`, check only the model(s)/field(s) directly referenced by this task. For existing service files, only open files whose names or exports are explicitly mentioned in `tasks.md` for this task — do not scan the broader service directory speculatively.

4. STRICT TYPE SAFETY: Ensure absolute TypeScript type safety. For custom fields or third-party libraries (e.g. Auth.js v5), apply proper module augmentation/type extensions as required by strict compiler config. Double-check imports.

5. WORKSPACE EXECUTION: Execute file creation/modification directly in the workspace — do not output markdown code blocks for manual copy-paste.

EXECUTION PIPELINE:
- Step 1: Parse the pasted TASK CONTENT above — no file read needed for this step.
- Step 2: Targeted lookup in `requirements.md`/`design.md` (scoped to this task's domain, or use Context/Notes path if provided).
- Step 3: Formulate implementation plan based on what was read.
- Step 4: Modify/create files directly in the repository.
- Step 5: Run type-check ONCE after all edits are complete — not after each individual file change. Fix any errors found, then re-run once more to confirm clean.

Proceed with Step 1 immediately. No fluff, just execute the task accurately according to the specs.