Act as a Principal Software Engineer specializing in Next.js 16, React 19, TypeScript, and Prisma 7.8.0. We are building the foundational data layer and infrastructure for "Project005 Task Management DSS" (a Decision Support System for academic task management). 

Your objective is to fully implement Task 1.1, 1.2, and 1.4 in a single, high-fidelity response. Do not abbreviate code, do not use placeholder comments like "// ... rest of your code", and ensure strict TypeScript typing throughout.

Here is the exact specification for each section:

### 1. DATABASE LAYER & PRISMA SCHEMA (Task 1.1 & 1.2)
Create a comprehensive `prisma/schema.prisma` configured for a PostgreSQL database (Neon Serverless). The schema must implement the following architectural constraints:
- Multi-tenant Many-to-Many task architecture via a explicit bridge table named `UserTaskProgress` to prevent data duplication when tasks are shared across classrooms.
- Supported Enums: 
  * `TaskStatus` (e.g., PENDING, IN_PROGRESS, COMPLETED)
  * `CookedTier` (MAIN_CHARACTER, LET_HIM_COOK, SLIGHTLY_COOKED, OVERCOOKED)
  * `PostTag` (CurhatTugas, ButuhTemanTim, TanyaJawaban, DiskusiUmum)
  * `DeliveryStatus` (SENT, FAILED)
  * `UserRole` (USER, ADMIN)
- Required Models & Relations:
  1. `User`: id, email (unique), name, passwordHash, role, locale, digestEnabled, digestTime, deliveryChannel, whatsappNumber, telegramChatId, createdAt, updatedAt. Relations to Session, UserTaskProgress, ClassRoomMember, CookedScore, AnonymousPost (encrypted tracking), DailyDigestLog.
  2. `Session`: Auth.js v5 standard fields.
  3. `Task`: id, title, description, sksWeight, taskWeight, deadlineAt, isSubTask (boolean), parentTaskId (self-relation optional), classRoomId (optional ClassRoom relation), creatorId, createdAt, updatedAt. Relations to UserTaskProgress, TaskOverride, TaskEditLog.
  4. `UserTaskProgress` (Bridge Table): userId, taskId, status, position (Int, optional for manual sorting override), completedAt, currentStressScore. Composite primary key: [userId, taskId].
  5. `ClassRoom`: id, className, classCode (unique, 8 chars), sksWeight, creatorId, createdAt. Relations to ClassRoomMember, Task.
  6. `ClassRoomMember`: classRoomId, userId, joinedAt. Composite primary key: [classRoomId, userId].
  7. `CookedScore`: id, userId, cumulativeScore, tier (CookedTier), date (DateTime). Composite unique constraint: [userId, date].
  8. `TaskOverride`: id, userId, taskId, reason, overriddenAt.
  9. `TaskEditLog`: id, taskId, editorId, fieldName, oldValue, newValue, editedAt.
  10. `AnonymousPost`: id, classRoomId, encryptedAuthorId (String/Text for RSA-OAEP), content (max 500 chars), tag (PostTag), createdAt.
  11. `DailyDigestLog`: id, userId, digestDate (DateTime), deliveryStatus, errorMessage, sentAt. Composite unique constraint: [userId, digestDate].
- DB Optimization: Add explicit indexes for `classRoomId`, `creatorId`, `deadlineAt`, and `parentTaskId`.

### 2. PROJECT STRUCTURE & TYPESCRIPT CONFIG (Task 1.4)
Provide the standard Boilerplate Configuration files and output a clear directory tree showing where core logical layers reside.
- Provide a clean `tsconfig.json` utilizing modern path aliases: `@/*` pointing to `./src/*`.
- Provide a complete directory tree mapping for:
  * `src/lib/services/` (priority-engine, auth, task, classroom, feed, cooked-meter, recovery-mode, daily-digest)
  * `src/lib/validation/` (Zod schemas)
  * `src/lib/errors/` (Domain-specific error handling)
  * `src/app/` (Next.js App Router structural layout)

Output the files directly in markdown blocks ready for implementation.