[COMMAND: AUTOMATED DIAGNOSTIC & PROBLEM SOLVING FOR src/lib/db.ts]

AGENT ROLE:
Act as an Expert TypeScript & Prisma 7 Engineer with deep debugging capabilities. Your task is to diagnose, analyze, and completely solve all current compilation and type errors inside `src/lib/db.ts`.

DIAGNOSTIC PROCESS:
1. Read the current contents of `src/lib/db.ts` and inspect all active TypeScript / compilation errors.
2. Verify that all required dependencies are present (such as `@prisma/adapter-neon`, `@neondatabase/serverless`, and `ws`).

COMMON ISSUES YOU MUST CHECK AND FIX:
- Global Scope Typing: Ensure `globalThis.prisma` is properly declared via `declare global { var prisma: ... | undefined }` so TypeScript does not throw an implicit 'any' or missing property error.
- WebSocket Integration: Ensure `neonConfig.webSocketConstructor = ws` is correctly evaluated only in server environments (e.g., `if (typeof window === "undefined")`) to prevent execution errors, and check if `ws` default vs named imports are causing type issues.
- Prisma 7 Extension Inference: Prisma 7's `$extends` has very strict type requirements. Ensure the query interception hooks (`query: { task: { ... }, userTaskProgress: { ... }, cookedScore: { ... } }`) match the exact types exported by `@/generated/prisma`. Use explicit, accurate type signatures for the arguments if automatic inference breaks.
- AsyncLocalStorage Type Context: Ensure that `AsyncLocalStorage` is correctly typed to hold either a string (`userId`) or an object, and that the fallback when the store is undefined is gracefully handled without causing runtime crashes or strict null checks errors.

STRICT EXECUTION RULES:
1. DO NOT output partial code or placeholders like `// ... rest of code`.
2. DO NOT just explain the solution in chat; you must directly update and rewrite `src/lib/db.ts` with the fully fixed, production-ready, type-safe code.
3. Ensure the final code compiles flawlessly under strict TypeScript rules.

EXECUTE NOW: Analyze the current errors in `src/lib/db.ts` and rewrite the file with the complete, error-free implementation.